specVersion: "v1"
serviceId: "omniyette"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "omniyette-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
